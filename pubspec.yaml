name: feature_gen
description: A command-line tool for generating feature modules.
version: 1.1.0

environment:
  sdk: ^3.10.4

dependencies:
  path: ^1.9.0
  args: ^2.7.0
  mustache_template: ^2.0.3
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  feature_gen:
